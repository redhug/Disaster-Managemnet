{
  "scripts": {
    "heroku-prebuild": "cd EOC && npm install && npm run build && cd .. && cd api && npm install",
    "start": "cd EOC && node server.js"
  },
  "engines": {
    "node": "8.9.2"
  }
}